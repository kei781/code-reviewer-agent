# Shared Boundary

Shared modules contain small TypeScript primitives that are stable across phases.

Avoid adding business-specific behavior here; prefer `src/domain/**` for policy and review semantics.
